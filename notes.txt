i want to try writing a script executor like the ones seen for roblox but for the ugc gaming platform vortex (playvortex.io), its a copy of roblox, and just like roblox, it uses luau for scripting.
i have experience writing lua and its derivitives like luau but ive never implemented or worked with the lua virtual machine closely, altough its easy to infer what it is and does.
i assumed that the script executor finds and hooks into the luau virtual machine to inject luau bytecode directly into the running game client.
i couldnt find any info on exploiting a lua virtual machine to inject bytecode, nor could i find any meaningful roblox specific information.
after asking google gemini it put together a basic overview of how it works for roblox, supporting what i thought before, altough a roblox executor also does some interesting things to elevate
privelages and what code can run.
here is the overview it gave me for roblox script executors.
1. Process Memory Injection
Before interacting with Luau, the executor must break into the Roblox process (RobloxPlayerBeta.exe).
User-Mode / Driver Injection: The executor application uses memory allocation APIs (VirtualAllocEx, WriteProcessMemory) or kernel-mode drivers to map a Dynamic Link Library (DLL) into Roblox’s address space.
Execution Hijacking: By spawning a thread (CreateRemoteThread) or hooking an existing thread inside the game process, the injected DLL gains the ability to execute C/C++ code directly inside Roblox's memory space.
2. Locating the Luau State (lua_State)
Once executing inside Roblox's memory, the injected DLL must find where the game engine stores its scripting engine.
Pattern Scanning (AOB): The DLL scans Roblox's memory for specific byte sequences (signatures) to locate internal functions and global pointers.
lua_State Retrieval: It identifies the lua_State structure pointer, which holds the active execution stack, global variables, garbage collector state, and registered C functions.
3. Escalating Script Security Identity (Capabilities)
Roblox isolates code using Security Identities (Levels 1 through 8). Normal game local scripts run at Level 2, preventing them from modifying critical game data or calling restricted functions.
Memory Patching: The injected DLL accesses the ExtraSpace or userdata offset linked to the active lua_State thread.
Capability Privilege Elevation: It directly overwrites the thread's permission bitmask, upgrading the script's identity to Level 7 or 8 (CoreScript / Plugin level). This grants the thread unrestricted permission to invoke methods like game:GetService("CoreGui") or access internal engine objects.
4. Bytecode Compilation and Loading
The Luau Virtual Machine does not execute raw text strings directly; it only reads binary bytecode.
Luau Compiler: The executor's UI takes the user's custom script text and compiles it into Luau bytecode formatted specifically for Roblox's modified opcode structure.  
Passing to the Loader: The injected DLL invokes Roblox's internal bytecode loading function (e.g., luau_load or luavm::load) directly from process memory. This converts the compiled bytecode buffer into a callable Luau closure (Closure*) inside the VM.
5. Task Scheduler Integration
Running foreign code from an unmanaged thread would cause memory corruption or instant game crashes due to race conditions with Roblox's render and physics loops.
Thread Creation: The DLL calls lua_newthread / luaL_sandboxthread to allocate a safe, isolated thread from the global lua_State.
Scheduling: The closure is pushed onto Roblox's internal Task Scheduler queue. The main game loop then executes the injected script safely on the next frame update alongside regular client scripts.
6. Environment Augmentation & Metamethod Detouring
To expand what the injected scripts can do, the executor modifies the script's global environment (getfenv).
Injecting Custom APIs: The DLL registers custom C functions into the global table, providing script authors with extra utilities like getgenv(), decompile(), firetouchinterest(), and request().
Metatable Hooking: The executor hooks into the engine's central metatable methods (such as __namecall or __index). This allows injected scripts to intercept, block, or rewrite network traffic and RemoteEvent calls before they reach Roblox's network layer.
when i start something i dont know much about i like to just follow general guidence even if i am not 100% sure why or what every step is doing or working towards, but it helps me have a basic
judgement of the work and time required for the project.
the version of vortex i am working with, its client, is vortex version 0.5.4 (Vortex-0.5.4-13.09.26.exe) (sha256 hash: 958C6C45DABEFC55EC87E45FE5ECBBD4424F44990AEF71D290B34114421D35F7).
but i fist i am curious to see if there are any decent open source script executors for roblox which implement the hooking and injection logic themselves instead of just using a pre-made api, in
reality when searching for technical information tutorials just use pre-made apis.
i can also try to find one of the apis used by the script executors if they are open source.
having a look on github and having google gemini analyse 2 different executors it showed that they operate in varying ways, i also got google gemini to find me some information on lua virtual
machine exploitation and bytecode injection. altough the lua and luau virtual machines differ a good amount so the same methodology doesnt work.
i compiled the outputs from a couple different gemini chats into one big prompt so i have some general guidance for the start, like i said before ill follow a general guide first.
using ghidra 12.1.2 i opened the vortex client executable and did the automated scan.
logically i know i am going to be comparing a lot of compiled and decompiled pseudo-c code to the luau compiled assembly and source code, so first i want to find out what version the vortex
client uses.
searching 'lua' in string search i got 'DEFINED	146cdcff0		ds "$Lua: Lua 5.1.4 Copyright (C) 1994-2008 Lua.org, PUC-Rio $\n$Authors: R. Ierusalimschy, L. H. de Figueiredo & W. Celes $\n$URL: www.lua.org $\n"	"$Lua: Lua 5.1.4 Copyright (C) 1994-2008 Lua.org, PUC-Rio $\n$Authors: R. Ierusalimschy, L. H. de Figueiredo & W. Celes $\n$URL: www.lua.org $\n"	string	141	true'
this makes sense as i think luau is built off lua 5.1.4, but thats not what we are looking for. luau doesnt use standard version numbers like Lua 5.1 or 5.3, instead it uses rolling commits and
a luau bytecode version integer defined in 'Common/include/Luau/Bytecode.h', i assume its this:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// Bytecode tags, used internally for bytecode encoded as a string
enum LuauBytecodeTag
{
    // Bytecode version; runtime supports [MIN, MAX], compiler emits TARGET by default but may emit a higher version when flags are enabled
    LBC_VERSION_MIN = 3,
    LBC_VERSION_MAX = 14,
    LBC_VERSION_TARGET = 9,
    LBC_VERSION_CLASSES = 100,
    // Type encoding version
    LBC_TYPE_VERSION_MIN = 1,
    LBC_TYPE_VERSION_MAX = 3,
    LBC_TYPE_VERSION_TARGET = 3,
    // Types of constant table entries
    LBC_CONSTANT_NIL = 0,
    LBC_CONSTANT_BOOLEAN,
    LBC_CONSTANT_NUMBER,
    LBC_CONSTANT_STRING,
    LBC_CONSTANT_IMPORT,
    LBC_CONSTANT_TABLE,
    LBC_CONSTANT_CLOSURE,
    LBC_CONSTANT_VECTOR,
    LBC_CONSTANT_TABLE_WITH_CONSTANTS,
    LBC_CONSTANT_INTEGER,
    LBC_CONSTANT_CLASS_SHAPE,
    LBC_CONSTANT_VECTORD,

    /** WARNING: This must always be last. */
    LBC_CONSTANT__COUNT
};
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
this presumably is used so luau code written for a different unsuported version will not run. searching for uses of 'LBC_VERSION_' in the repo we get 'VM/src/lvmload.cpp' and
'Bytecode/src/BytecodeBuilder.cpp'.
the source file, lvmload.cpp, is a part of the luau virtual machine implementation and is responsible for loading and parsing bytecode into executable prototypes, it serves as the interface
between the compiled bytecode blobs and the vm's internal execution structures, ensuring that the binary layout is correctly interpreted before execution begins. 
looking at the source code the 'LBC_VERSION_' variables are only used for one thing:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    if ((version < LBC_VERSION_MIN || version > LBC_VERSION_MAX) && version != LBC_VERSION_CLASSES) 														< USED HERE
    {
        char chunkbuf[LUA_IDSIZE];
        const char* chunkid = luaO_chunkid(chunkbuf, sizeof(chunkbuf), chunkname, strlen(chunkname));
        lua_pushfstring(L, "%s: bytecode version mismatch (expected [%d..%d], got %d)", chunkid, LBC_VERSION_MIN, LBC_VERSION_MAX, version);				< USED HERE
        return 1;
    }
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
we see its used to throw an error message when there is a luau bytecode and virtual machine mismatch, altough it doesnt really matter because we have already found something useful.
here we have a error message, a string, which we can search for in the binary within ghidra.
for now we will ignore the second source file utilising these variables.
searching for the string "%s: bytecode version mismatch (expected [%d..%d], got %d)" in ghidra we get 1 hit on the address '146cdcec8', the address/string has one xref pointing to it, '1466f0f05':
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
       1466f0f05 48 8d 15        LEA        RDX,[0x146cdcec8]											: [s_%s:_bytecode_version_mismatch_(e_146cdcec8]
                 bc bf 5e 00
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
that error message string only appears in one place in the source code, lvmload.cpp. meaning that we are now looking at the decompiled instructions of the previous code block from lvmload.cpp.
in the windows 64 calling convention rdx is used to pass the second integer, pointer, or aggregate argument to a function, this function being the lua_pushfstring function, as seen the string is
the second argument after 'L'.
looking at the decompiled pseudo-c code we have:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - FUN_1466efe60 -
  if (3 < (byte)(cVar2 - 3U)) {
    sVar7 = strlen(param_2);
    uVar8 = FUN_146662ea0(&uStack_148,0x100,param_2,sVar7);
    FUN_1466ecb80(param_1,"%s: bytecode version mismatch (expected [%d..%d], got %d)",uVar8,3,
                  CONCAT44(uVar39,6),cVar2);
    return 1;
  }
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
which lines up with the source code extract from lvmload.cpp, presumably meaning that our bytecode version should be 3 because thats what alligns with the 'version' variables place.
but thats not the case, because the compiler optimises '((version < LBC_VERSION_MIN || version > LBC_VERSION_MAX) && version != LBC_VERSION_CLASSES)' down to '(3 < (byte)(cVar2 - 3U))'.
the expression (byte)(cVar2 - 3U) subtracts 3 from cVar2 and truncates the result to 8 bits, the condition 3 < ... then tests whether that byte value is greater than 3.
its just a very optimised range check on a character value, that character being 'cVar2'/'version', so we know 'version' is 'cVar2' here, now we need to find out what the value of it is.
i honestly really dont think this is going to lead anywhere meaningful because i assume the bytecode version is very wide/general but its fun practice anyways.
now we middle click 'cVar2' in the pseudo-c decompilation window, which highlights every usage of the variable, now we can see a bit higher that the last assignment of our variable 'cVar2' is
'cVar2 = *param_3;', this is the only assignment. now highlighting param_3 we can see its one of the functions (the function we are currently in) arguments, which its also obvious from the name.
'undefined8 FUN_1466efe60(longlong param_1,char *param_2,char *param_3,int param_4,int param_5)'.
from the assignment we can see '*param_3' is a pointer because of the '*', so whats happening is that the pointer is being de-referenced and its value is being asigned to 'cVar2'.
from the function arguments we see 'char *param_3', so we also know that param_3 is a pointer to a char data type.
referencing our decompilation to the source code, we see that 'cVar2 = *param_3;' lines up with 'uint8_t version = read<uint8_t>(data, size, offset);'.
i do not have enough theory or experience to know what is happening, remember that ghidra tries to decompile this c++ code as c code, so its messy.
but searching it up, in c++, <uint8_t> inside read<uint8_t>(...) is a template argument. the template function read<T> uses this parameter to specify that it should parse (read) 1 byte from
the input buffer as an unsigned 8-bit integer (uint8_t).
quick note, both char and uint8_t types occupy exactly 1 byte (8 bits) in memory, in x86/x64 c++ compilers, char is typically signed by default (range -128 to 127), whereas uint8_t (an alias
for unsigned char) is explicitly unsigned (0 to 255).
in read<uint8_t>, the <uint8_t> tells the compiler to generate a version of the read function where the return type T is uint8_t. internally that function performs a pointer read/cast,
such as reinterpret_cast<const uint8_t*>) to extract the byte.
in the decompilation the value param_3 dereferences to is a char (signed 1 byte data type, default char), but its being returned and assigned to cVar2 as a uint8_t (unsigned 1 byte data type,
uint8_t is typically just a typedef for unsigned char).
ghidra likely typed cVar2 as an unsigned 8-bit integer because the assembly uses 8-bit zero-extension (movzx) or unsigned arithmetic instructions on that specific variable.
tracing back the data variable, since thats what the function which assigns cVar2 its value reads from, you get 'const char* data', so yes the data variable in
'uint8_t version = read<uint8_t>(data, size, offset);' is a signed char and we convert it into a unsigned char.
at the assembly level, reading param_3 executes an 8-bit memory move (such as mov al, byte ptr [rdx]), because the operation simply moves 8 raw bits from memory into a register,
no extra conversion instructions are required, the distinction between signed and unsigned only affects subsequent instructions like movzx (zero-extend) or arithmetic checks.
well yeah in the end this isnt that useful (for now) but for fun, in '((version < LBC_VERSION_MIN || version > LBC_VERSION_MAX) && version != LBC_VERSION_CLASSES)' from '(3 < (byte)(cVar2 - 3U))'
we know that LBC_VERSION_MIN is 3 and LBC_VERSION_MAX is 6, and we know that because The decompiled condition if '(3 < (byte)(cVar2 - 3U))' embeds both bounds directly.
when a compiler optimizes a range check like 'if (version < MIN || version > MAX)' into a single comparison, it compiles into this pseudocode pattern:
'if (3/SPAN < (byte)(cVar2/variable - 3/MIN))'.
extracting the minimum (MIN): look directly at the value being subtracted inside the parentheses (variable - MIN).
subtracting the lower bound shifts the minimum valid input down to 0 (3 - 3 = 0). Any value smaller than MIN underflows an unsigned byte to a large number (2 - 3 = 255),
automatically failing the check. in the code (cVar2 - 3U), The subtracted number is 3.
extracting the maximum (MAX): take the comparison limit outside the parentheses (SPAN) and add it to MIN, (MAX = MIN + SPAN).
the value outside the parentheses represents the maximum allowed offset range above 0. Adding the offset limit (SPAN) back to the starting point (MIN}) yields the upper bound.
in the code (3 < ...), the comparison limit is 3. MAX = 3 + 3 = 6.
so we know the version of luau used in vortex accepts luau bytecode versions 3 to 6, but the actual value of version (cVar2) comes from the first byte of the data variable in source code.
the data varaible, i think, being a pointer to the first byte in the actual luau bytecode prepared for execution in the virtual machine.
from my basic understanding from reading the source code and looking some stuff up, i havent actually read any documentation for some reason, im pretty sure the luau_load function actually just
loads a chunk of luau bytecode (pretty sure generated by luau_compile using normal luau code) into a lua state, returning a callable function (a closure) that can be executed via lua_pcall.
okay i read a bit about it and the general execution loop for embedded luau in c/c++ is this:
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// compile source luau code into bytecode.
size_t bytecodeSize = 0;
char* bytecode = luau_compile(source, strlen(source), NULL, &bytecodeSize);
// load bytecode into the VM stack, pushes closure, creating an executable function object inside the luau VM and placing a reference to it at the very top of luaus virtual evaluation stack.
if (luau_load(L, chunkname, bytecode, bytecodeSize, 0) == 0) {
    // execute the closure
    lua_pcall(L, 0, 0, 0); 
}
// free bytecode from heap.
free(bytecode);
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
ignoring functions made for use in luau itself and internal runtime functions, only focusing on normal embedded luau in c/c++ usage that is the standard execution flow.
the basic flow is compile, load, call.
note, this minimal example doesnt include a lot of the boilerplate code like loading global luau functions, error handling, closing the luau VM, ect.
but the most important thing it leaves out is defining the lua_State, a lua_State is created like this: 'lua_State* L = luaL_newstate();'.
this creates a new lua_State struct object on the heap using the luaL_newstate() function, which L is a pointer to.
the most important thing for us, if we want to exploit the luau virtual machine, is the lua_State. so we need a basic understanding of what the lua_State is.
every function that works with the luau virtual machine requires you to pass a reference to the lua_State object, above you see it passed as 'L', thats how its defined in source code.
at its core lua_State represents a single thread of execution (a coroutine in the luau vm, scripting environment) in the luau vm.
whenever you call a c api function like lua_pushnumber(L, 10) or lua_pcall(L, ...), the lua_State* L parameter tells the VM which thread context, evaluation stack, and memory space you are
operating on.
reading about lua_State i also learned of global_State, which is also very important. a single luau vm instance contains exactly one global_State, but can have multiple lua_State objects sharing
that global state. previously i assumed all you have to do is make a lua_State instance and thats the entire luau virtual machine instance, but thats not everything.
ive just read a basic overview i havent looked into source code deeply so this might not be fully accurate but it should.
when creating a new lua_State struct object with luaL_newstate() it automatically creates the global_State struct object alongside the lua_State object and its thread.
all threads created via lua_newthread(L) (or coroutine.create() in luau vm scripting environment) point back to this single global_State struct, which owns the garbage collector, string table,
registry table and primitive metatables.
this is how it all works, when creating the lua_State with luaL_newstate(), the vm performs a single combined allocation in memory that creates both the root global_State and the primary
lua_State (the main thread).
global_State is allocated once, it owns all state that must persist across the entire application lifecycle (strings, memory allocators, global registry, and the garbage collector).
the main lua_State is created, its internal pointer L->global (dereference pointer to lua_State struct (L) and access the member variable named global inside it) is pointed directly at the newly
created global_State. global_State->mainthread is pointed back to this primary lua_State.
when lua_newthread(L) (or coroutine.create in a luau script) is called, Luau does not create a new global_State, instead it allocates a lightweight child lua_State.
it basically inherits the engine, the new child lua_State struct has its internal global pointer set to L->global (copying the address of the existing global_State).
what is isolated, it allocates its own execution stack array and call frame tracking buffer.
parent relationship, the new lua_State object is pushed onto the stack of the parent lua_State as a value of type LUA_TTHREAD.
now with this basic understanding and warmup we need to get back to our main goal, our main goal is to inject our own bytecode into the luau vm.
the methodology above outlines the most popular option of this type of exploitation, we map (inject) a dll into the target process, retreive the lua_State struct pointer (likely L), 
in roblox it escalates the script security identity (not sure if this applies here in same way or at all), to load the bytecode the dll invokes the internal bytecode loading function
(for eg luau_load or luavm::load) directly from process memory, this converts the compiled bytecode buffer into a callable luau closure (Closure*) inside the vm, to avoid memory corruption
the dll creates a new thread, the DLL calls lua_newthread / luaL_sandboxthread to allocate a safe and isolated thread from the global lua_State, The closure is then pushed onto the games
internal task scheduler queue, The main game loop then executes the injected script safely on the next frame update alongside regular client scripts, the dll can also modify the global
environment to inject custom functions or intercept different game functions.
quick not, as i go on now in ghidra i will be renaming functions and variables, for eg, FUN_1466ecb80 as lua_pushfstring and param_1 as L. i am also bookmarking named functions.
also, i downloaded and hashed the newest version of vortex, 0.5.5, as Vortex-0.5.5-20.09.26.exe, its hash is C7F76D8FD80B0BBBDCA4BA092B8F1556EA8D21FE12CFD4F3630107AD22F71288.
for now i will continue working with the old version.
well getting back to the goal, our first goal is to be able to find and steal the lua_State pointer (L). there are a few ways.
since the lua_State pointer is shared between every child lua_State thread, it just points back to the main lua_State thead, this even applies for uses of it in the actual luau vm
implementation and use of it in your own ebeddeding code.
we can simply get any function that has the pointer as one of its arguments and then retreieve it live by hooking onto the function and saving the argument, a purpose built library like tinyhook
could be used but i would do it on my own.
thats one way, another way could be to use the injected dll to execute built in functions to retreive it for us, idk if this would work but ive seen examples of it, altough they do include more
complex logic like decryption.
for now i think the first option would be better, to learn and for a result. but with any of those methods i need to learn how to write a dll, write a dll injector, then work closely with the
program utilising the win32 api.
bruh im ip banned from guidedhacking.com but ive never even been there wtf?
well from my basic understanding we need to write a dll that runs in the target vortex process, that will spin off its own thread to do what we need. but first we need to write an injector,
we need to map our dll into the active process somehow, well with win32 api.
i made a new clion project to learn the basic windows api stuff and whatnot, i configured it to use the microsoft visual studio toolchain and made sure its on release.
the project is in 'CLion Project - win32 api and memory research'.
